# Stop (and thus unmount) the squashfs lockbox when a USB partition disappears.
ACTION=="remove", SUBSYSTEM=="block", ENV{ID_BUS}=="usb", ENV{DEVTYPE}=="partition", \
  RUN+="/usr/bin/systemctl --no-block stop squashfs-automount.service"
